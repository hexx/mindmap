---
title: "org-modeインポート/エクスポートのエッジケーステストを追加する"
status: TODO
created: 2026-07-07T16:01:00+09:00
---

# 課題解決プロンプト: org-modeインポート/エクスポートのエッジケーステストを追加する

## 1. 役割定義 (Persona)
あなたはデータのインポート/エクスポート処理のテスト設計に精通したソフトウェアエンジニアです。現在1件しかない org-mode 入出力のテストを拡充し、エッジケースでの安全性を確保してください。

## 2. 背景・前提条件 (Context)

### 現状
- **ファイル**: `src/utils/orgModeGraph.test.ts`
- **既存テスト**: 1件（LEFT/RIGHTタグ付きの round-trip のみ）
- **未テストの重要ケース**: 多数

### 対象ファイルと責務
- `src/utils/exportOrgMode.ts` — MindMapNode/Edge のグラフ → org-mode 文字列
- `src/utils/importOrgMode.ts` — org-mode 文字列 → MindMapNode/Edge のグラフ
- `src/utils/orgModeGraph.ts` — Heading[] → グラフ構築（importの中核ロジック）

### 追加すべきテストケース

#### exportOrgMode
| # | ケース | 検証内容 |
|---|-------|---------|
| 1 | ルートのみの空グラフ | 空文字列が返らない / ルートの見出しのみ出力 |
| 2 | 深さ3以上のネスト構造 | 見出しレベルが正しく1,2,3...とネストする |
| 3 | 空ラベルノード | "無題" にフォールバックされる |
| 4 | 改行を含むラベル | 正規化されて出力される |
| 5 | 特殊文字（日本語、記号）を含むラベル | 文字化けしない |
| 6 | LEFT/RIGHTの振り分け | 右側（x>0）は :RIGHT:、左側（x<0）は :LEFT: タグが付与される |

#### importOrgMode
| # | ケース | 検証内容 |
|---|-------|---------|
| 7 | 空文字列 | 空グラフ？例外？→ 仕様を明確にしてテスト |
| 8 | 見出しなし（本文のみ） | ルートのみのグラフになる |
| 9 | 深いネスト | 正しい親子関係のエッジが生成される |
| 10 | 不正なorg-mode構文 | 例外が発生するか、適切にハンドリングされる |
| 11 | :LEFT:/:RIGHT:タグ付き見出し | タグが除去され、位置情報に変換される（既存テストの拡張） |

#### round-trip（往復）
| # | ケース | 検証内容 |
|---|-------|---------|
| 12 | 複雑なツリー構造の往復 | export→import でノード数・エッジ数・親子関係が保存される |
| 13 | 改行・特殊文字を含むラベルの往復 | 情報が失われない |

### 関連ファイル
- `src/utils/orgModeGraph.test.ts`（既存、拡張）
- `src/utils/exportOrgMode.ts`
- `src/utils/importOrgMode.ts`
- `src/utils/orgModeGraph.ts`

## 3. 解決すべきゴール (Goal & Objective)
- [ ] `src/utils/orgModeGraph.test.ts` に上記のテストケースを追加する（既存の1件を残したまま拡張）
- [ ] テストに必要なヘルパー（`createNode`, `createEdge`）が既存のものを使えるか確認し、必要なら共通化する（`useStore.test.ts` にも同様のヘルパーが存在）
- [ ] `npx vitest run` で全件パスすることを確認する
- [ ] テストケースごとに `describe` ブロックで `exportOrgMode` / `importOrgMode` / `round-trip` に分類する

## 4. 期待する成果物 (Output Format)
- 修正ファイル: `src/utils/orgModeGraph.test.ts`
- `npx vitest run` の実行結果（全件パス、テストケース一覧を含む）

## 5. 実行開始の合図
このプロンプトを理解したら、「タスクを開始します」と宣言し、直ちに `exportOrgMode.ts` と `orgModeGraph.ts` の全コードパスの洗い出しから始めてください。
