現在のマインドマップアプリのテスト（src/store/useStore.test.ts および tests/mindmap.spec.ts）に対して、これまでの開発で実装された高度な機能のうち、まだテストされていない以下の重要なユースケースを追加してください。

【要件1: Vitest (useStore.test.ts) への追加ケース】
以下のストアロジックを検証するテストケースを `describe('useStoreのテスト')` 内に追加してください。
1. **左右への自動整列（applyAutoLayout）**: ルートノードの子ノードを3つ以上追加した後に `applyAutoLayout` を実行すると、右側（x座標 > 0）と左側（x座標 < 0）の両方にノードが振り分けられること。
2. **グラフのインポート**: `importGraph` にダミーのノードとエッジを渡して実行すると、状態が上書きされ、`currentCloudMindmapId` が `null` にリセットされること。
3. **クラウド保存（saveToCloud）のモックテスト**: ファイル上部でモックされている `client.api` のレスポンスを `vi.mocked` 等で設定し、`saveToCloud` 実行後に `currentCloudMindmapId` がモックしたAPIのレスポンスIDで正しく更新されること。

【要件2: Playwright (tests/mindmap.spec.ts) への追加ケース】
以下のUI/UXの挙動を検証するテストケースを `test.describe('マインドマップのUIテスト')` 内に追加してください。
1. **ダブルクリックでの編集モード**: ルートノードをダブルクリック（`dblclick`）すると、即座にインライン編集モード（input要素）に切り替わること。
2. **スペースキーでの編集モード**: 選択状態のノードで Space キーを押すと、インライン編集モードに切り替わること。
3. **モバイルツールバーの動作**: 
   - テスト内で `page.setViewportSize({ width: 375, height: 667 })` などを呼び出し、モバイルビューにする。
   - 画面下部にモバイルツールバー（`.mobile-toolbar`）が表示されていることを確認する。
   - ルートノードを選択した状態でツールバーの「子ノード追加」ボタンをタップ（またはクリック）すると、新しいノードが追加されること。
   - ルートノードを選択中は「兄弟ノード追加」「削除」ボタンが `disabled` になっていること。

各テストケースには、何をしているかが分かるように明確なテスト名（it/testの第一引数）と、適切なArrange/Act/Assertのコメントを記述してください。
